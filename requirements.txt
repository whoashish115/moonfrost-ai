# Model and training
torch>=2.6.0
numpy

# Tokenizer: training it and loading it
tokenizers

# Benchmark datasets, and the reference models scored on the same harness
datasets
transformers

# Chat server
fastapi
uvicorn[standard]
pydantic

# Odds and ends used by the data downloader and the cloud runner
requests
tqdm
